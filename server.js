document.addEventListener('DOMContentLoaded', () => {
    const alertOverlay = document.getElementById('security-layer');
    if (alertOverlay) {
        alertOverlay.style.display = 'flex';
        alertOverlay.style.opacity = '1';
    }
});
